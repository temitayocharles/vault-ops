# 00 Prerequisites

Tools:
- `python3`
- `kubectl`
- Vault reachable at your chosen address

Required env vars:

```bash
export VAULT_ADDR=<vault-address>
export VAULT_USERNAME=<vault-user>
export VAULT_PASSWORD=<vault-password>
```
