apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault-cila-health-microservices-clinical-intake
spec:
  provider:
    vault:
      server: http://vault.vault.svc:8200
      path: kv
      version: v2
      auth:
        kubernetes:
          mountPath: kubernetes
          role: staging-cila-health-microservices-clinical-intake
          serviceAccountRef:
            name: eso-cila-health-microservices-clinical-intake
            namespace: external-secrets
